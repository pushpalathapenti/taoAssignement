package com.tao.taskManager.utils;

/*
 * public enum Priority { HIGH("high"), MEDIUM("medium"), LOW("low"); private
 * final String name; Priority(String priority) { name = priority; } }
 */

public enum Priority {
    HIGH,
    MEDIUM,
    LOW;
    
    // ...
}